services:
  db:
    image: postgres:16
    container_name: crbad_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crbad
    ports:
      - "5431:5432"
    volumes:
      - crbad_pgdata:/var/lib/postgresql/data

volumes:
  crbad_pgdata:

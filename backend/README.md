# Backend (API)

Source of truth for the API lives in this folder.

- Local Docker Compose dev workflow: see the root README (`../README.md`).
- Local (non-Docker) dev: `npm install` then `npm run dev`.
